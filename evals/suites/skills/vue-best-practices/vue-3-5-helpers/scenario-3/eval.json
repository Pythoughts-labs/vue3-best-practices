{
  "skills": [
    "vue-best-practices"
  ],
  "query": "Make an SSR-safe Vue 3.5 labelled input using the built-in id helper instead of a hand-rolled counter.",
  "files": [
    "src/components/SignupField.vue"
  ],
  "expected_behavior": [
    "Imports and uses useId() from vue for the field id",
    "Binds :for and :id to the same generated id",
    "Does not hand-roll id generation (Math.random / global counter / Date.now)"
  ]
}
